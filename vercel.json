{
    "builds": [{
        "src": "mkdocs.yml",
        "use": "@vercel/static-build",
        "config": {
            "distDir": "site"
        }
    }],
    "buildCommand": "pip install -r doc-requirements.txt && mkdocs build",
    "outputDirectory": "site"
}