{
    "builds": [{
        "src": "build.sh",
        "use": "@vercel/static-build",
        "config": {
            "distDir": "site"
        }
    }],
    "buildCommand": "bash ./build.sh",
    "outputDirectory": "site"
}