{
    "buildCommand": "mkdocs build",
    "outputDirectory": "site",
    "framework": null,
    "installCommand": "pip install -r doc-requirements.txt",
    "builds": [
        {
            "src": "**",
            "use": "@vercel/python"
        }
    ]
}
